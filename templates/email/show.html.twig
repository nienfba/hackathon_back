{% extends 'base.html.twig' %}

{% block title %}Email{% endblock %}

{% block body %}
    <h1>Email</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ email.id }}</td>
            </tr>
            <tr>
                <th>EmailFrom</th>
                <td>{{ email.emailFrom }}</td>
            </tr>
            <tr>
                <th>EmailTo</th>
                <td>{{ email.emailTo ? email.emailTo|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>EmailSubject</th>
                <td>{{ email.emailSubject }}</td>
            </tr>
            <tr>
                <th>EmailContent</th>
                <td>{{ email.emailContent }}</td>
            </tr>
            <tr>
                <th>EmailType</th>
                <td>{{ email.emailType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('email_index') }}">back to list</a>

    <a href="{{ path('email_edit', {'id': email.id}) }}">edit</a>

    {{ include('email/_delete_form.html.twig') }}
{% endblock %}
