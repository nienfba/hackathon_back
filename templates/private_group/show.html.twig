{% extends 'base.html.twig' %}

{% block title %}Groupe Privé{% endblock %}

{% block body %}
<h1 class="Admin">Groupe Privé</h1>

<table class="table Admin">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ private_group.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ private_group.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ private_group.description }}</td>
        </tr>
    </tbody>
</table>
<div class="Admin centre margin">
    <a href="{{ path('private_group_index') }}">back to list</a>

    <a href="{{ path('private_group_edit', {'id': private_group.id}) }}">edit</a>
</div>
<div class="Admin centre margin">
    {{ include('private_group/_delete_form.html.twig') }}
</div>
{% endblock %}