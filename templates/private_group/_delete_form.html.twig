<form method="post" action="{{ path('private_group_delete', {'id': private_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ private_group.id) }}">
    <button class="btn-danger" title="Supprimer"><i class="fas fa-trash"></i></button>
</form>
