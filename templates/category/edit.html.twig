{% extends 'base.html.twig' %}

{% block title %}Edition d'une Catégorie{% endblock %}

{% block body %}
    <h1 class="Admin">Edition d'une Catégorie</h1>
<div class="Admin margin">
    {{ include('category/_form.html.twig', {'button_label': 'Sauve'}) }}
</div>
<div class="Admin centre margin">
    <a href="{{ path('category_index') }}"><button class="btn" title="Retour à la liste"><i class="fas fa-reply"></i></button></a>

    {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
