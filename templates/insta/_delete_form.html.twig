                    <form method="post" class="InstAdmin" action="{{ path('insta_delete', {'id': instum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                    <input type="hidden" name="_method" value="DELETE">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instum.id) }}">
                    <button class="btn-danger" title="Supprimer"><i class="fas fa-trash"></i></button>
                    </form>
