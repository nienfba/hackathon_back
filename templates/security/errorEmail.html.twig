{% extends 'base-public.html.twig' %}

{% block main %}

    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">ERREUR !</h1>
            <hr class="my-3">
            <p class="lead">L'adresse email saisie n'est pas pas repertorié sur notre site.</p>
            <p class="lead">Merci d'utiliser l'adresse email saisie lors de l'inscription !!</p>
            <hr class="my-3">
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ path('password-revovery') }}" role="button">Mot de passe oublié</a>
            </p>
        </div>
    </div>

{% endblock %}

{# :<strong>{{email}}</strong> #}