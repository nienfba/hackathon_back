{% extends 'base.html.twig' %}

{% block title %}Membres{% endblock %}

{% block body %}
    <h1 class="Admin">Membres</h1>

    <table class="table Admin">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ member.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ member.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ member.password }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ member.email }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ member.role }}</td>
            </tr>
        </tbody>
    </table>
<div class="Admin centre margin">
    <a href="{{ path('member_index') }}"><button class="btn" title="Retour à la liste"><i class="fas fa-reply"></i></button></a>
    <a href="{{ path('member_edit', {'id': member.id}) }}"><button class="btn" title="Editer"><i class="fa fa-pen"></i></button></a>
</div>
<div class="Admin centre margin">
    {{ include('member/_delete_form.html.twig') }}
    </div>
{% endblock %}
